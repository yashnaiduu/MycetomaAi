backbone:
  name: resnet50
  pretrained: true
  attention: cbam

heads:
  classification:
    num_classes: 3
    dropout: 0.3
  detection:
    output_dim: 4
  subtype:
    num_subtypes: 10
    dropout: 0.4

ssl:
  simclr_dim: 2048
  projection_dim: 128
  dino_model: facebook/dinov2-small
